<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Package-visibility queries (Android 11+ / targetSdk >= 30).

        When this app is launched by the RESPECT launcher and relays xAPI
        statements back, TangyCache.forwardXapiStatements() binds to the
        launcher's XapiIpcService using an implicit intent with action
        "org.openeel.action.xapioveripc" targeting package world.respect.app.

        Without declaring that service here, Android package-visibility
        filtering makes the resolution fail with
        "Unable to start service ... not found" and the statements never reach
        the launcher's LRS.
    -->
    <queries>
        <intent>
            <action android:name="org.openeel.action.xapioveripc" />
        </intent>
        <intent>
            <action android:name="org.openeel.action.httpoveripc.connect" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link intent filters for RESPECT launch URLs -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
            </intent-filter>
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
            </intent-filter>

            <!--
                Open Educational Experience Launcher (RESPECT) launch support.
                RESPECT fires an intent with the org.openeel.action.LAUNCH action
                and the lesson URL as its data when it detects this app is
                installed. Because the intent uses a custom action (not
                ACTION_VIEW) it does NOT require a verified App Link, so it works
                even when the Tangerine server is selected at runtime and its
                domain is not declared in this manifest.
            -->
            <intent-filter>
                <action android:name="org.openeel.action.LAUNCH" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:scheme="http" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Camera: photo & video field types -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Microphone: audio field types & video capture -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Location: geo / location field types -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Hardware features used by survey field types (optional, so the app stays
         installable on devices without these sensors). -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
</manifest>
